{
  "name": "singlehosting",
  "private": true,
  "version": "1.0.0",
  "description": "Static hosting app",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node -e \"console.log('No build step required for static files.')\"",
    "build:azure": "npm run build"
  }
}
